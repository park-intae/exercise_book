DROP TABLE USERS;

CREATE TABLE USERS (
  ID VARCHARS(12) PRIMARY KEY,
  PASSWORD VARCHARS(12) NOT NULL,
  NAME VARCHARS(30) NOT NULL,
  ROLE VARCHARS(6) NOT NULL
);


INSERT INTO USERS(ID, PASSWORD, NAME, ROLE)
VALUES('guest', 'guest123', '방문자', 'USER');

INSERT INTO USERS(ID, PASSWORD, NAME, ROLE)
VALUES('admin', 'admin123', '관리자', 'ADMIN');

INSERT INTO USERS(ID, PASSWORD, NAME, ROLE)
VALUES('member', 'member123', '일반회원', 'USER');

COMMIT

SELECT * FROM USERS;

